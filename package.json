{
  "name": "anwalive",
  "version": "0.1.0",
  "private": true,
  "description": "AnwaLive production-oriented monorepo foundation",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w apps/api",
    "test": "npm run test -w apps/api",
    "lint": "npm run lint -w apps/api"
  }
}
